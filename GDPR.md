# GDPR Compliance Implementation

Реализовано GDPR-совместимое решение для управления cookie consent на сайте nanosudo.com.

## Реализованные компоненты

### 1. Cookie Consent Component

**Расположение:** `src/components/cookie-consent/cookie-consent.tsx`

**Особенности:**
- ✅ Полная поддержка всех языков (ru, en, kk) через next-intl
- ✅ Кнопка "Отклонить" (обязательно для GDPR)
- ✅ Кнопка "Принять все"
- ✅ Настройка предпочтений с категориями cookies
- ✅ Сохранение предпочтений в localStorage
- ✅ Кастомное событие для интеграции с аналитикой
- ✅ Доступность (ARIA labels, keyboard navigation)
- ✅ Адаптивный дизайн

**Категории cookies:**
- **Необходимые** (always enabled) - для работы сайта
- **Аналитические** (optional) - для аналитики использования
- **Маркетинговые** (optional) - для персонализации и рекламы

### 2. Privacy Policy Page

**Расположение:** `src/app/[locale]/privacy-policy/page.tsx`

**Содержание:**
- Введение
- Сбор данных
- Использование cookies
- Использование данных
- Защита данных
- Права пользователей
- Контактная информация

**Особенности:**
- Полная поддержка всех языков
- SEO-оптимизация (metadata)
- Структурированный контент
- Ссылка в футере

### 3. Cookie Consent Utilities

**Расположение:** `src/lib/cookie-consent.ts`

**Функции:**
- `getCookieConsent()` - получить текущие настройки
- `hasConsentFor(category)` - проверить согласие для категории
- `hasAcceptedAll()` - проверить, приняты ли все cookies
- `hasDeclinedAll()` - проверить, отклонены ли все cookies
- `clearCookieConsent()` - очистить настройки (для тестирования)

## Интеграция

### В Layout

Компонент `CookieConsent` автоматически отображается на всех страницах через `src/app/[locale]/layout.tsx`:

```tsx
<NextIntlClientProvider messages={messages}>
  {children}
  <CookieConsent />
</NextIntlClientProvider>
```

### Использование в коде

```tsx
import { hasConsentFor } from "@/lib/cookie-consent";

// Проверить согласие для аналитики
if (hasConsentFor("analytics")) {
  // Инициализировать Google Analytics
}

// Проверить согласие для маркетинга
if (hasConsentFor("marketing")) {
  // Инициализировать рекламные скрипты
}
```

### Интеграция с аналитикой

Компонент отправляет кастомное событие при изменении согласия:

```tsx
window.addEventListener("cookieConsent", (event) => {
  const { status, preferences } = event.detail;
  
  if (preferences.analytics) {
    // Включить аналитику
    gtag('consent', 'update', {
      'analytics_storage': 'granted'
    });
  } else {
    // Отключить аналитику
    gtag('consent', 'update', {
      'analytics_storage': 'denied'
    });
  }
});
```

## GDPR Compliance Checklist

✅ **Обязательные элементы:**
- [x] Кнопка "Отклонить" (не только "Принять")
- [x] Ссылка на политику конфиденциальности
- [x] Категории cookies (необходимые, аналитика, маркетинг)
- [x] Cookies не устанавливаются до согласия
- [x] Возможность настройки предпочтений
- [x] Сохранение выбора пользователя
- [x] Понятное описание каждой категории

✅ **Технические требования:**
- [x] Многоязычная поддержка
- [x] Доступность (ARIA, keyboard navigation)
- [x] Адаптивный дизайн
- [x] SEO-оптимизация
- [x] Утилиты для проверки согласия

## Переводы

Все тексты переведены на:
- 🇷🇺 Русский (ru)
- 🇺🇸 English (en)
- 🇰🇿 Қазақша (kk)

**Файлы переводов:**
- `messages/ru.json` - секция `cookieConsent` и `privacyPolicy`
- `messages/en.json` - секция `cookieConsent` и `privacyPolicy`
- `messages/kk.json` - секция `cookieConsent` и `privacyPolicy`

## Ссылки

- Privacy Policy: `/privacy-policy` (или `/en/privacy-policy`, `/kk/privacy-policy`)
- Ссылка в футере: автоматически добавлена в `SiteFooter`

## Дальнейшие улучшения

Возможные улучшения в будущем:
- Интеграция с Google Analytics 4
- Интеграция с другими аналитическими сервисами
- Автоматическое удаление cookies при отказе
- Экспорт/импорт настроек пользователя
- Статистика согласий

## Тестирование

Для тестирования можно использовать утилиты:

```tsx
import { clearCookieConsent, getCookieConsent } from "@/lib/cookie-consent";

// Очистить согласие (показать баннер снова)
clearCookieConsent();

// Проверить текущее согласие
const consent = getCookieConsent();
console.log(consent);
```
